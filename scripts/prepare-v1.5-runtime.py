from __future__ import annotations

import re
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]


def read(rel: str) -> str:
    return (ROOT / rel).read_text(encoding="utf-8-sig")


def write(rel: str, text: str) -> None:
    path = ROOT / rel
    path.parent.mkdir(parents=True, exist_ok=True)
    encoding = "utf-8-sig" if path.suffix.lower() in {".ps1", ".psm1"} else "utf-8"
    path.write_text(text, encoding=encoding)


def once(text: str, old: str, new: str, label: str) -> str:
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{label}: expected 1 match, found {count}")
    return text.replace(old, new, 1)


def regex_once(text: str, pattern: str, new: str, label: str) -> str:
    result, count = re.subn(pattern, lambda _m: new, text, count=1, flags=re.S)
    if count != 1:
        raise SystemExit(f"{label}: expected 1 match, found {count}")
    return result


runtime = read("Runtime-PROX2-AutoSwitch.ps1")

runtime = once(
    runtime,
    "# Timeouts de G HUB (ms). Overridables desde config.json.\n",
    '''# Logitech PRO X 2 LIGHTSPEED direct Centurion HID provider.\n# DetectionMode remains LogitechGHub for backward-compatible config files;\n# PRO X 2 uses direct HID before the legacy G HUB battery provider.\n$script:CenturionModulePath = Join-Path $InstallDir "lib\\LogitechProX2Centurion.psm1"\n$script:CenturionAvailable = $false\nif (Test-Path $script:CenturionModulePath) {\n    try {\n        Import-Module $script:CenturionModulePath -ErrorAction Stop\n        $script:CenturionAvailable = $true\n    }\n    catch {\n        $script:CenturionAvailable = $false\n    }\n}\n\n# G HUB timeouts (ms). Overridable from config.json.\n''',
    "runtime Centurion import",
)

status_helpers = '''# --- Provider state shared from worker -> tray ---\n# Hardware polling remains in the worker. The WinForms thread only reads this\n# small status file, so HID/G HUB calls never block the tray.\nfunction Write-HeadsetProviderStatus {\n    param(\n        [Parameter(Mandatory = $true)][string]$Provider,\n        [Parameter(Mandatory = $true)][string]$State,\n        [int]$BatteryPercent = -1\n    )\n\n    try {\n        $control = Join-Path $InstallDir "control"\n        New-Item -ItemType Directory -Path $control -Force | Out-Null\n        $path = Join-Path $control "headset-status.json"\n        $tmp = "$path.tmp"\n        [ordered]@{\n            Provider       = $Provider\n            State          = $State\n            BatteryPercent = $BatteryPercent\n            UpdatedAt      = (Get-Date).ToString("o")\n        } | ConvertTo-Json -Compress | Set-Content -Path $tmp -Encoding UTF8\n        Move-Item $tmp $path -Force\n    }\n    catch { }\n}\n\nfunction Get-HeadsetProviderStatus {\n    try {\n        $path = Join-Path (Join-Path $InstallDir "control") "headset-status.json"\n        if (-not (Test-Path $path)) { return $null }\n        $status = Get-Content -Raw -Path $path | ConvertFrom-Json\n        if (-not $status.UpdatedAt) { return $null }\n        $age = (Get-Date) - [datetime]$status.UpdatedAt\n        if ($age.TotalSeconds -gt 15) { return $null }\n        return $status\n    }\n    catch { return $null }\n}\n\n'''
runtime = once(
    runtime,
    "# --- Tray icon + Timer (message pump) + worker process ---\n",
    status_helpers + "# --- Tray icon + Timer (message pump) + worker process ---\n",
    "runtime provider status helpers",
)
runtime = once(
    runtime,
    "$script:MenuItemInfoHeadset = $null\n$script:MenuItemInfoFallback = $null\n",
    "$script:MenuItemInfoHeadset = $null\n$script:MenuItemInfoBattery = $null\n$script:MenuItemInfoFallback = $null\n",
    "runtime battery menu variable",
)

new_tray_info = '''function Update-TrayInfo {\n    $hs = if ($Config.HeadsetName) { [string]$Config.HeadsetName } else { [string]$Config.HeadsetId }\n    $fb = if ($Config.SpeakerName) { [string]$Config.SpeakerName } else { [string]$Config.SpeakerId }\n    $providerStatus = Get-HeadsetProviderStatus\n\n    if ($script:MenuItemInfoHeadset) {\n        $suffix = ""\n        if ($providerStatus -and $providerStatus.State) {\n            $suffix = " — $($providerStatus.State)"\n        }\n        $script:MenuItemInfoHeadset.Text = "Headset: $hs$suffix"\n        $script:MenuItemInfoHeadset.Enabled = $false\n    }\n\n    if ($script:MenuItemInfoBattery) {\n        $batteryText = "Battery: n/a"\n        if ($providerStatus -and $providerStatus.Provider -eq "Centurion") {\n            if ($providerStatus.State -eq "Connected" -and [int]$providerStatus.BatteryPercent -ge 0) {\n                $batteryText = "Battery: $([int]$providerStatus.BatteryPercent)%"\n            }\n            elseif ($providerStatus.State -eq "Disconnected") {\n                $batteryText = "Battery: -- (Disconnected)"\n            }\n            else {\n                $batteryText = "Battery: -- (Unknown)"\n            }\n        }\n        $script:MenuItemInfoBattery.Text = $batteryText\n        $script:MenuItemInfoBattery.Enabled = $false\n    }\n\n    if ($script:MenuItemInfoFallback) {\n        $script:MenuItemInfoFallback.Text = "Fallback: $fb"\n        $script:MenuItemInfoFallback.Enabled = $false\n    }\n\n    $next = "Current: ?"\n    try {\n        $current = Get-DefaultRenderItemId\n        if ($current -ieq [string]$Config.HeadsetId) { $next = "Next switch: $fb" }\n        elseif ($current -ieq [string]$Config.SpeakerId) { $next = "Next switch: $hs" }\n        else { $next = "Current: $current" }\n    }\n    catch { $next = "Next switch: unknown" }\n    if ($script:MenuItemInfoNext) {\n        $script:MenuItemInfoNext.Text = $next\n        $script:MenuItemInfoNext.Enabled = $false\n    }\n\n    if ($script:TrayIcon) {\n        $tip = "Audio AutoSwitch"\n        if ($providerStatus -and $providerStatus.Provider -eq "Centurion") {\n            if ($providerStatus.State -eq "Connected" -and [int]$providerStatus.BatteryPercent -ge 0) {\n                $tip = "PRO X 2 - $([int]$providerStatus.BatteryPercent)% - AutoSwitch"\n            }\n            elseif ($providerStatus.State -eq "Disconnected") {\n                $tip = "PRO X 2 - Disconnected - AutoSwitch"\n            }\n        }\n        if ($tip.Length -gt 60) { $tip = $tip.Substring(0, 60) }\n        $script:TrayIcon.Text = $tip\n    }\n}\n\n'''
runtime = regex_once(
    runtime,
    r"function Update-TrayInfo \{.*?\n\}\n\n# --- Reconfigure headset/fallback without reinstalling ---",
    new_tray_info + "# --- Reconfigure headset/fallback without reinstalling ---",
    "runtime Update-TrayInfo",
)

runtime = once(
    runtime,
    '    [void]$menu.Items.Add($script:MenuItemInfoHeadset)\n\n    $script:MenuItemInfoFallback = New-Object System.Windows.Forms.ToolStripMenuItem\n',
    '''    [void]$menu.Items.Add($script:MenuItemInfoHeadset)\n\n    $script:MenuItemInfoBattery = New-Object System.Windows.Forms.ToolStripMenuItem\n    $script:MenuItemInfoBattery.Text = "Battery: n/a"\n    $script:MenuItemInfoBattery.Enabled = $false\n    [void]$menu.Items.Add($script:MenuItemInfoBattery)\n\n    $script:MenuItemInfoFallback = New-Object System.Windows.Forms.ToolStripMenuItem\n''',
    "runtime battery tray item",
)

new_logitech_worker = '''        else {\n            # Backward-compatible LogitechGHub config: PRO X 2 uses direct\n            # Centurion HID; other compatible Logitech headsets keep G HUB.\n            $useCenturion = $script:CenturionAvailable -and\n                (Test-LogitechProX2CenturionConfig -Config $Config)\n\n            if ($useCenturion) {\n                try {\n                    $centurion = Get-LogitechProX2CenturionState\n                    if ($centurion.State -eq "Connected") {\n                        $isOn = $true; $known = $true\n                        Write-HeadsetProviderStatus -Provider "Centurion" -State "Connected" -BatteryPercent ([int]$centurion.BatteryPercent)\n                        if ($availabilityLogged) { Write-AutoSwitchLog "PRO X 2 Centurion HID provider recovered." }\n                        $availabilityLogged = $false\n                    }\n                    elseif ($centurion.State -eq "Disconnected") {\n                        $isOn = $false; $known = $true\n                        Write-HeadsetProviderStatus -Provider "Centurion" -State "Disconnected"\n                        $availabilityLogged = $false\n                    }\n                    else {\n                        $misses = 0\n                        Write-HeadsetProviderStatus -Provider "Centurion" -State "Unknown"\n                        if (-not $availabilityLogged) {\n                            Write-AutoSwitchLog (("PRO X 2 Centurion state is unknown: {0}. " + "The output will not be changed.") -f $centurion.Error)\n                            $availabilityLogged = $true\n                        }\n                    }\n                }\n                catch {\n                    $misses = 0\n                    Write-HeadsetProviderStatus -Provider "Centurion" -State "Unknown"\n                    if (-not $availabilityLogged) {\n                        Write-AutoSwitchLog (("PRO X 2 Centurion provider unavailable: {0}. " + "It will retry; unknown state never changes the output.") -f $_.Exception.Message)\n                        $availabilityLogged = $true\n                    }\n                }\n            }\n            else {\n                try {\n                    if (-not $script:WorkerBatteryPath) {\n                        Connect-GHub\n                        $script:WorkerBatteryPath = Get-ProX2BatteryPath\n                        if ($availabilityLogged) { Write-AutoSwitchLog "G HUB connection recovered." }\n                        else { Write-AutoSwitchLog "Connected to G HUB." }\n                        $availabilityLogged = $false\n                    }\n                    $battery = Invoke-GHubGet -Path $script:WorkerBatteryPath\n                    $isOn = $null -ne $battery.payload\n                    $known = $true\n                    Write-HeadsetProviderStatus -Provider "GHub" -State $(if ($isOn) { "Connected" } else { "Disconnected" })\n                }\n                catch {\n                    Close-GHubConnection\n                    $script:WorkerBatteryPath = $null\n                    $misses = 0\n                    Write-HeadsetProviderStatus -Provider "GHub" -State "Unknown"\n                    if (-not $availabilityLogged) {\n                        Write-AutoSwitchLog (("G HUB/AutoSwitch unavailable: {0}. " + "It will retry; while the state is unknown the output is not changed.") -f $_.Exception.Message)\n                        $availabilityLogged = $true\n                    }\n                }\n            }\n        }\n\n        if ($known) {'''
runtime = regex_once(
    runtime,
    r"        else \{\n            # LogitechGHub: PERSISTENT connection\..*?\n        \}\n\n        if \(\$known\) \{",
    new_logitech_worker,
    "runtime Logitech worker",
)

old_reconfigure = '''            else {\n                $answer = [System.Windows.Forms.MessageBox]::Show(\n                    "Windows does not reflect the physical ON/OFF cycle of this headset.`n`nIs it a Logitech PRO X 2 detected by G HUB?",\n                    "Audio AutoSwitch",\n                    [System.Windows.Forms.MessageBoxButtons]::YesNo,\n                    [System.Windows.Forms.MessageBoxIcon]::Question\n                )\n                if ($answer -eq 'Yes') {\n                    $lblStatus.Text = "Step 3/3: checking G HUB for PRO X 2..."\n                    $lblStatus.Refresh()\n                    $ghubDevice = Test-GHubProX2\n                    if ($ghubDevice) {\n                        $newMode = "LogitechGHub"\n                        $newGhubName = [string]$ghubDevice.extendedDisplayName\n                    }\n                }\n            }\n'''
new_reconfigure = '''            else {\n                if ($script:CenturionAvailable -and $newHeadsetName -match '(?i)PRO\\s*X\\s*2') {\n                    try {\n                        $lblStatus.Text = "Step 3/3: checking PRO X 2 direct HID..."\n                        $lblStatus.Refresh()\n                        $centurion = Get-LogitechProX2CenturionState\n                        if ($centurion.State -eq 'Connected' -or $centurion.State -eq 'Disconnected') {\n                            $newMode = "LogitechGHub"\n                            $newGhubName = $newHeadsetName\n                            Write-AutoSwitchLog "Reconfigure: PRO X 2 direct Centurion HID provider detected."\n                        }\n                    }\n                    catch { }\n                }\n\n                if (-not $newMode) {\n                    $answer = [System.Windows.Forms.MessageBox]::Show(\n                        "Windows does not reflect the physical ON/OFF cycle of this headset.`n`nIs it a Logitech headset detected by G HUB?",\n                        "Audio AutoSwitch",\n                        [System.Windows.Forms.MessageBoxButtons]::YesNo,\n                        [System.Windows.Forms.MessageBoxIcon]::Question\n                    )\n                    if ($answer -eq 'Yes') {\n                        $lblStatus.Text = "Step 3/3: checking G HUB..."\n                        $lblStatus.Refresh()\n                        $ghubDevice = Test-GHubProX2\n                        if ($ghubDevice) {\n                            $newMode = "LogitechGHub"\n                            $newGhubName = [string]$ghubDevice.extendedDisplayName\n                        }\n                    }\n                }\n            }\n'''
runtime = once(runtime, old_reconfigure, new_reconfigure, "runtime reconfigure provider")
write("Runtime-PROX2-AutoSwitch.ps1", runtime)


installer = read("Instalar-PROX2-AutoSwitch.ps1")
installer = once(installer, '$SteelModuleSrc = Join-Path $PackageDir "lib\\SteelSeriesNova5.psm1"\n', '$SteelModuleSrc = Join-Path $PackageDir "lib\\SteelSeriesNova5.psm1"\n$CenturionModuleSrc = Join-Path $PackageDir "lib\\LogitechProX2Centurion.psm1"\n', "installer Centurion source")
installer = once(installer, '@($RuntimeSrc, $UninstallSrc, $VerifySrc, $ModuleSrc, $SteelModuleSrc, $HelperSrc, $IconSrc)', '@($RuntimeSrc, $UninstallSrc, $VerifySrc, $ModuleSrc, $SteelModuleSrc, $CenturionModuleSrc, $HelperSrc, $IconSrc)', "installer required files")
installer = once(installer, 'Import-Module $ModuleSrc -ErrorAction Stop\n', 'Import-Module $ModuleSrc -ErrorAction Stop\nImport-Module $CenturionModuleSrc -ErrorAction Stop\n', "installer import provider")
installer = once(installer, 'Copy-Item $SteelModuleSrc (Join-Path $InstallDir "lib\\SteelSeriesNova5.psm1") -Force\n', 'Copy-Item $SteelModuleSrc (Join-Path $InstallDir "lib\\SteelSeriesNova5.psm1") -Force\nCopy-Item $CenturionModuleSrc (Join-Path $InstallDir "lib\\LogitechProX2Centurion.psm1") -Force\n', "installer copy provider")
installer = once(installer, '''    Write-Host "  [2] Any Logitech headset (G HUB)" -ForegroundColor White\n    Write-Host "      -> Use LogitechGHub: Windows keeps the endpoint Active even" -ForegroundColor DarkGray\n    Write-Host "         when OFF, so detection uses the G HUB battery signal." -ForegroundColor DarkGray\n''', '''    Write-Host "  [2] Logitech headset (PRO X 2 direct HID; others via G HUB)" -ForegroundColor White\n    Write-Host "      -> PRO X 2 reads its LIGHTSPEED receiver directly (Centurion HID)." -ForegroundColor DarkGray\n    Write-Host "         Other compatible Logitech headsets keep the G HUB provider." -ForegroundColor DarkGray\n''', "installer menu")

option2 = '''    elseif ($cycleChoice -eq 2) {\n        $centurionMatched = $false\n        if ($headsetName -match '(?i)PRO\\s*X\\s*2') {\n            try {\n                $centurion = Get-LogitechProX2CenturionState\n                if ($centurion.State -eq 'Connected' -or $centurion.State -eq 'Disconnected') {\n                    $DetectionMode = "LogitechGHub"\n                    $ghubHeadset = [pscustomobject]@{ extendedDisplayName = $headsetName; id = "centurion-direct" }\n                    $centurionMatched = $true\n                    $batteryDetail = if ([int]$centurion.BatteryPercent -ge 0) { " battery=$([int]$centurion.BatteryPercent)%" } else { "" }\n                    Write-Host "      PRO X 2 direct Centurion HID detected.$batteryDetail" -ForegroundColor Green\n                }\n            }\n            catch { Write-Host ("      Direct PRO X 2 HID check failed: {0}" -f $_.Exception.Message) -ForegroundColor DarkGray }\n        }\n\n        if (-not $centurionMatched) {\n            Write-Host "      Looking up the Logitech headset in G HUB..." -ForegroundColor Yellow\n            try {\n                Connect-GHub\n                $devices = Invoke-GHubGet -Path "/devices/list"\n                $ghubCandidates = @($devices.payload.deviceInfos | Where-Object { Test-LogitechHeadsetDevice -Device $_ })\n                if ($ghubCandidates.Count -eq 0) {\n                    Write-Host "      G HUB reports no compatible Logitech headset. Try option 3." -ForegroundColor Red\n                }\n                else {\n                    $ghubHeadset = $ghubCandidates[0]\n                    if ($ghubCandidates.Count -gt 1) {\n                        Write-Host "Logitech headsets detected by G HUB:" -ForegroundColor Yellow\n                        for ($i = 0; $i -lt $ghubCandidates.Count; $i++) {\n                            Write-Host ("  [{0}] {1}  ({2})" -f ($i + 1), $ghubCandidates[$i].extendedDisplayName, $ghubCandidates[$i].id)\n                        }\n                        $ghubChoice = 0\n                        do {\n                            $gc = Read-Host "Enter the number of the Logitech headset that matches '$headsetName'"\n                            $ghubValid = [int]::TryParse($gc, [ref]$ghubChoice) -and $ghubChoice -ge 1 -and $ghubChoice -le $ghubCandidates.Count\n                        } until ($ghubValid)\n                        $ghubHeadset = $ghubCandidates[$ghubChoice - 1]\n                    }\n                    $DetectionMode = "LogitechGHub"\n                    Write-Host "      G HUB: $($ghubHeadset.extendedDisplayName)" -ForegroundColor Green\n                }\n            }\n            catch { Write-Host ("      Could not connect to G HUB. Detail: {0}" -f $_.Exception.Message) -ForegroundColor Red }\n            finally { Close-GHubConnection }\n        }\n    }\n'''
installer = regex_once(installer, r"    elseif \(\$cycleChoice -eq 2\) \{.*?\n    \}\n    elseif \(\$cycleChoice -eq 4\) \{", option2 + '    elseif ($cycleChoice -eq 4) {', "installer Logitech option")
write("Instalar-PROX2-AutoSwitch.ps1", installer)


verifier = read("Verificar-PROX2-AutoSwitch.ps1")
verifier = once(verifier, 'Show-Test "Logic module (lib)" (Test-Path (Join-Path $InstallDir "lib\\AutoSwitchCore.psm1")) (Join-Path $InstallDir "lib\\AutoSwitchCore.psm1")\n', 'Show-Test "Logic module (lib)" (Test-Path (Join-Path $InstallDir "lib\\AutoSwitchCore.psm1")) (Join-Path $InstallDir "lib\\AutoSwitchCore.psm1")\nShow-Test "PRO X 2 Centurion module" (Test-Path (Join-Path $InstallDir "lib\\LogitechProX2Centurion.psm1")) (Join-Path $InstallDir "lib\\LogitechProX2Centurion.psm1")\n', "verifier module")

provider_checks = '''# Provider-specific checks.\nif ($mode -eq 'LogitechGHub') {\n    $centurionModule = Join-Path $InstallDir "lib\\LogitechProX2Centurion.psm1"\n    $useCenturion = $false\n    if ($cfg -and (Test-Path $centurionModule)) {\n        try {\n            Import-Module $centurionModule -ErrorAction Stop\n            $useCenturion = Test-LogitechProX2CenturionConfig -Config $cfg\n        }\n        catch { $useCenturion = $false }\n    }\n\n    if ($useCenturion) {\n        Write-Host "[SKIP] G HUB (PRO X 2 uses direct Centurion HID in v1.5.0)" -ForegroundColor DarkGray\n        try {\n            $direct = Get-LogitechProX2CenturionState\n            $ok = $direct.State -eq 'Connected' -or $direct.State -eq 'Disconnected'\n            $detail = "State=$($direct.State)"\n            if ([int]$direct.BatteryPercent -ge 0) { $detail += "; battery=$([int]$direct.BatteryPercent)%" }\n            if ($direct.Error) { $detail += "; $($direct.Error)" }\n            Show-Test "PRO X 2 direct HID state" $ok $detail\n        }\n        catch { Show-Test "PRO X 2 direct HID state" $false $_.Exception.Message }\n    }\n    else {\n        $ghubPort = $false\n        $ghubPortToTest = 9010\n        if ($cfg -and $cfg.PSObject.Properties['GHubPort'] -and $cfg.GHubPort) { $ghubPortToTest = [int]$cfg.GHubPort }\n        try {\n            $client = New-Object System.Net.Sockets.TcpClient\n            $iar = $client.BeginConnect("127.0.0.1", $ghubPortToTest, $null, $null)\n            $ghubPort = $iar.AsyncWaitHandle.WaitOne(1000, $false)\n            if ($ghubPort) { $client.EndConnect($iar) }\n            $client.Close()\n        }\n        catch { $ghubPort = $false }\n        $ghubDetail = if ($ghubPort) { "Port reachable" } else { "Open Logitech G HUB" }\n        Show-Test "G HUB localhost:$ghubPortToTest" $ghubPort $ghubDetail\n    }\n}\nelseif ($mode -eq 'WindowsEndpoint') {\n    Write-Host "[SKIP] G HUB (not used in WindowsEndpoint mode)" -ForegroundColor DarkGray\n}\nelseif ($mode -eq 'SteelSeriesNova5') {\n    Write-Host "[SKIP] G HUB (not used in SteelSeriesNova5 mode)" -ForegroundColor DarkGray\n    $steelModule = Join-Path $InstallDir "lib\\SteelSeriesNova5.psm1"\n    if (Test-Path $steelModule) {\n        try {\n            Import-Module $steelModule -ErrorAction Stop\n            if (Test-SteelSeriesNova5Receiver) { Show-Test "SteelSeries Nova 5/5X receiver" $true "HID receiver found" }\n            else { Show-Test "SteelSeries Nova 5/5X receiver" $false "No compatible HID receiver found" }\n        }\n        catch { Show-Test "SteelSeries Nova 5/5X receiver" $false $_.Exception.Message }\n    }\n    else { Show-Test "SteelSeries Nova 5/5X receiver" $false "Module not installed" }\n}\nelse {\n    Show-Test "Detection mode" $false "Could not read a valid DetectionMode from config.json"\n}\n\n'''
verifier = regex_once(verifier, r"# G HUB is a requirement only for the Logitech-specific detection path\..*?\nif \(Test-Path \$ConfigPath\) \{", provider_checks + 'if (Test-Path $ConfigPath) {', "verifier provider checks")
verifier = once(verifier, '''        if ($mode -eq 'LogitechGHub' -and $cfg.GHubDisplayName) {\n            Write-Host "  G HUB:          $($cfg.GHubDisplayName)"\n        }\n''', '''        if ($mode -eq 'LogitechGHub' -and $cfg.GHubDisplayName) {\n            $cm = Join-Path $InstallDir "lib\\LogitechProX2Centurion.psm1"\n            try {\n                if (Test-Path $cm) { Import-Module $cm -ErrorAction Stop }\n                if ((Test-Path $cm) -and (Test-LogitechProX2CenturionConfig -Config $cfg)) {\n                    Write-Host "  Provider:       PRO X 2 Centurion HID"\n                }\n                else { Write-Host "  G HUB:          $($cfg.GHubDisplayName)" }\n            }\n            catch { Write-Host "  G HUB:          $($cfg.GHubDisplayName)" }\n        }\n''', "verifier provider display")
write("Verificar-PROX2-AutoSwitch.ps1", verifier)


build = read("scripts/build-release.sh")
build = once(build, "'lib/AutoSwitchCore.psm1','lib/SteelSeriesNova5.psm1','tools/Test-SteelSeriesNova5Hid.ps1'", "'lib/AutoSwitchCore.psm1','lib/LogitechProX2Centurion.psm1','lib/SteelSeriesNova5.psm1','tools/Test-LogitechProX2Centurion.ps1','tools/Test-SteelSeriesNova5Hid.ps1'", "release paths")
write("scripts/build-release.sh", build)

check = read("scripts/check-repository.py")
check = once(check, 'CURRENT_VERSION = "1.4.0"', 'CURRENT_VERSION = "1.5.0"', "current version")
check = once(check, '    "lib/AutoSwitchCore.psm1",\n', '    "lib/AutoSwitchCore.psm1",\n    "lib/LogitechProX2Centurion.psm1",\n    "tools/Test-LogitechProX2Centurion.ps1",\n', "required provider files")
write("scripts/check-repository.py", check)

validate = read(".github/workflows/validate.yml").replace("1.2.5", "1.5.0")
write(".github/workflows/validate.yml", validate)


diagnostic = '''#requires -Version 5.1\n$ErrorActionPreference = "Stop"\n\n$Root = Split-Path -Parent $PSScriptRoot\n$Module = Join-Path $Root "lib\\LogitechProX2Centurion.psm1"\nif (-not (Test-Path $Module)) { throw "Centurion module not found: $Module" }\nImport-Module $Module -Force -ErrorAction Stop\n\nWrite-Host ""\nWrite-Host "=== Logitech PRO X 2 Centurion HID diagnostic ===" -ForegroundColor Cyan\nWrite-Host "VID 046D / PID 0AF7 / UsagePage FFA0" -ForegroundColor DarkGray\nWrite-Host "Press Ctrl+C to stop." -ForegroundColor Cyan\nWrite-Host ""\nwhile ($true) {\n    $r = Get-LogitechProX2CenturionState\n    $battery = if ([int]$r.BatteryPercent -ge 0) { "$([int]$r.BatteryPercent)%" } else { "n/a" }\n    Write-Host ("State={0} Battery={1} OfflineSignature={2}" -f $r.State, $battery, $r.OfflineSignatureSeen)\n    if ($r.Error) { Write-Host "Detail: $($r.Error)" -ForegroundColor DarkYellow }\n    if ($r.Frames) { Write-Host "Frames: $($r.Frames)" -ForegroundColor DarkGray }\n    Start-Sleep -Seconds 1\n}\n'''
write("tools/Test-LogitechProX2Centurion.ps1", diagnostic)

print("v1.5 runtime preparation OK")
